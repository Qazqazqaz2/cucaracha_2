# TON Jetton Search Bot

Telegram бот для поиска и анализа TON токенов (Jettons) с поддержкой инлайн-поиска.

## Возможности

- 🔍 **Инлайн-поиск токенов** - найдите любой TON токен прямо в Telegram
- 📊 **Анализ пулов ликвидности** - информация о ценах, ликвидности и комиссиях
- 🏦 **Поддержка множественных DEX** - STON.fi и DeDust.io
- 📈 **Актуальные данные** - автоматическое обновление цен каждые 5 минут
- 💾 **PostgreSQL база данных** - надежное хранение данных о токенах и пулах
- 🔄 **Система бэкапов** - автоматическое сохранение в GitHub

## Архитектура

Проект состоит из двух основных компонентов:

### 1. Telegram Bot (`no_main.py`)
- **Инлайн-поиск**: Поиск токенов по символу или названию
- **Форматированный вывод**: Красивое отображение информации о токенах
- **Интеграция с БД**: Получение данных из PostgreSQL

### 2. Индексатор (`indexator.py`)
- **Сбор данных**: Автоматический сбор пулов с STON.fi и DeDust.io
- **Обновление цен**: Получение актуальных цен через TON API
- **Фильтрация**: Отбор только активных пулов с достаточной ликвидностью
- **База данных**: Сохранение метаданных токенов и информации о пулах

## Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/Qazqazqaz2/cucaracha_2.git
cd cucaracha_2
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте PostgreSQL:**
```sql
CREATE DATABASE cryptoindexator;
CREATE USER postgres WITH PASSWORD '762341';
GRANT ALL PRIVILEGES ON DATABASE cryptoindexator TO postgres;
```

4. **Настройте переменные окружения:**
```bash
# В файле no_main.py и indexator.py обновите:
BOT_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
DB_DSN = "postgresql://postgres:password@localhost:5432/cryptoindexator"
TONAPI_KEY = "YOUR_TON_API_KEY"
```

## Использование

### Запуск индексатора
```bash
python indexator.py
```
Индексатор будет:
- Собирать пулы с STON.fi и DeDust.io
- Обновлять цены токенов через TON API
- Фильтровать неактивные пулы
- Обновлять данные каждые 5 минут

### Запуск бота
```bash
python no_main.py
```

### Использование бота
1. Найдите бота в Telegram
2. В любом чате введите: `@YourBotName jetton <название токена>`
3. Выберите нужный токен из результатов поиска

## Структура базы данных

### Таблица `jettons`
- `address` - адрес токена
- `symbol` - символ токена (например, USDT)
- `name` - полное название
- `price_ton` - цена в TON
- `price_usd` - цена в USD
- `liquidity` - ликвидность в USD
- `total_supply` - общее количество токенов
- `decimals` - количество десятичных знаков

### Таблица `pools`
- `pool_address` - адрес пула
- `token0_address` - адрес первого токена
- `token1_address` - адрес второго токена
- `platform` - платформа (ston/dedust)
- `price_per_ton` - цена токена в TON
- `lp_total_supply` - общее количество LP токенов
- `protocol_fee_percent` - комиссия протокола

## API интеграции

- **TON API** - получение метаданных и цен токенов
- **STON.fi API** - данные о пулах и ликвидности
- **DeDust.io API** - альтернативный источник данных о пулах

## Система бэкапов

### Создание бэкапа
```bash
python backup_script.py backup
# или
backup.bat backup
```

### Восстановление из бэкапа
```bash
python backup_script.py restore
# или
backup.bat restore
```

### Просмотр истории
```bash
python backup_script.py history
# или
backup.bat history
```

## Конфигурация

Основные настройки в коде:
- `REFRESH_INTERVAL = 300` - интервал обновления (секунды)
- `CONCURRENCY_LIMIT = 10` - лимит одновременных запросов
- `TONAPI_RATE_LIMIT = 1` - ограничение скорости TON API

## Мониторинг

Логи показывают:
- Количество обработанных пулов
- Ошибки API запросов
- Статистику фильтрации неактивных пулов
- Время обновления данных

## Требования

- Python 3.8+
- PostgreSQL 12+
- Telegram Bot Token
- TON API Key

## Лицензия

MIT License
